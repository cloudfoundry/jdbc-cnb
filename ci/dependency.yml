---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: latest

inputs:
- name: jdbc-cnb
- name: mariadb-jdbc
- name: postgresql-jdbc

outputs:
- name: jdbc-cnb

caches:
- path: go-module-cache

run:
  path: jdbc-cnb/ci/dependency.sh

params:
  GIT_USER_EMAIL:
  GIT_USER_NAME:
